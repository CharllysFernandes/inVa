{
  "name": "browser-extension",
  "version": "1.0.0",
  "description": "Extensão para Chrome/Edge",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --dev",
    "package": "node build.js --package"
  },
  "devDependencies": {
    "fs-extra": "^11.0.0"
  }
}
